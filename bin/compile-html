#!/usr/bin/env coffee

fs = require 'fs'
path = require 'path'
assets = require('../assets')


assets.html (error, html) ->
  throw error if error
  destination_path = path.join(__dirname, '../public/app.html')
  fs.writeFile destination_path, html, (error) ->
    throw error if error
