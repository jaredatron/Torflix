#!/usr/bin/env coffee

fs = require 'fs'
path = require 'path'
assets = require('../assets')


assets.javascript 'client', (error, asset) ->
  throw error if error
  destination_path = path.join(__dirname, '../public/app.js')
  asset.pipe fs.createWriteStream(destination_path)
