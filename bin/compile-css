#!/usr/bin/env coffee

fs = require 'fs'
path = require 'path'
assets = require('../assets')


assets.stylesheet 'app', (error, css) ->
  throw error if error
  destination_path = path.join(__dirname, '../public/app.css')
  fs.writeFile destination_path, css, (error) ->
    throw error if error
